import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
    main: {
        height: '100%',
        padding: '50px',
        width: '80%',
        minWidth: '800px',
    },
})
